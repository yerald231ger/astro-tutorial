---

---
<div class="fixed top-0 left-0 right-0 bg-[#0f172a] border-b border-gray-700 z-10 h-16">
  <!-- Center search bar -->

  <div class="flex items-center h-full ml-64">
    <!-- Added margin to account for sidebar -->
    <div class="flex-1 max-w-3xl px-4">
      <div class="relative">
        <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
          <svg class="h-5 w-5 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path>
          </svg>
        </div>
        <input
          type="search"
          class="block w-full pl-10 pr-20 py-2 bg-gray-800 border border-gray-600 rounded-md text-gray-200 placeholder-gray-400 focus:outline-none focus:border-emerald-500"
          placeholder="Find something..."
        />
        <div class="absolute inset-y-0 right-0 pr-3 flex items-center pointer-events-none">
          <span class="text-gray-400 text-sm border border-gray-600 rounded px-1.5 py-0.5">⌘K</span>
        </div>
      </div>
    </div>

    <!-- Right side navigation -->
    <div class="flex items-center space-x-4 px-4">
      <a href="#" class="text-gray-300 hover:text-white">API</a>
      <a href="#" class="text-gray-300 hover:text-white">Documentation</a>
      <a href="#" class="text-gray-300 hover:text-white">Support</a>
      <button class="px-4 py-2 text-sm font-medium text-white bg-emerald-500 rounded-md hover:bg-emerald-600">
        Sign in
      </button>
    </div>
  </div>
</div>
